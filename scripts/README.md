# Scripts

Put runnable scripts here as you write them by hand.

Suggested script responsibilities:

- Data download and verification.
- Tokenizer training runs.
- Dataset encoding.
- Model training.
- Generation from checkpoints.
- Experiment plotting.

Keep scripts configurable and record the exact command for each experiment in `writeup/experiment_log.md`.

